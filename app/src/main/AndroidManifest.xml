<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kozakiewicz.szymon.julianonandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activites.AnswerActivity"></activity>
        <activity android:name=".activites.DoRepetitionActivity" />
        <activity android:name=".activites.AddNewQuestionActivity" />
        <activity android:name=".activites.RepeatListActivity" />
        <activity android:name=".activites.AddRepetitionActivity" />
        <activity android:name=".activites.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>